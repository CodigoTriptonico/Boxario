/** Default page size for inventory movements lists (server-side). */
export const INVENTORY_MOVEMENTS_PAGE_SIZE = 50;

/** Hard cap for `listInventoryMovementsAction` limit. */
export const INVENTORY_MOVEMENTS_MAX_LIMIT = 200;
